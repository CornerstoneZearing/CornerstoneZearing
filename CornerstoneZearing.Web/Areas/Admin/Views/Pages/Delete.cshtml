@model CornerstoneZearing.Data.Entities.Page
@{
    ViewData["Section"] = "Pages";
    ViewData["Title"] = "Delete Page";
}

<div class="page-header">
    <h1>Delete Page</h1>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Back
    </a>
</div>

<div class="card" style="max-width:520px;">
    <div class="card-body">
        <p class="text-body-secondary mb-4">
            Are you sure you want to permanently delete this page? This action cannot be undone.
        </p>
        <div class="bg-body-tertiary border rounded-3 p-3 mb-4">
            <div style="font-weight:600;margin-bottom:.25rem;">@Model.Name</div>
            <div class="text-muted">/@(ViewBag.FullPath)</div>
            <div style="margin-top:.5rem;">
                @switch (Model.Status)
                {
                    case CornerstoneZearing.Data.PageStatus.Published:
                        <span class="badge badge-success">Published</span>
                        break;
                    case CornerstoneZearing.Data.PageStatus.Draft:
                        <span class="badge badge-warning">Draft</span>
                        break;
                    case CornerstoneZearing.Data.PageStatus.Withdrawn:
                        <span class="badge badge-secondary">Withdrawn</span>
                        break;
                }
            </div>
        </div>
        <div class="form-actions">
            <form asp-action="Delete" asp-route-id="@Model.PageID" method="post">
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-trash"></i> Yes, Delete Page
                </button>
            </form>
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</div>
